// This is app.js (The new, simplified version)

// --- Main DOMContentLoaded ---
document.addEventListener('DOMContentLoaded', () => {
    
    // เรียกใช้ฟังก์ชัน initializeApp() 
    // ซึ่งตอนนี้อาศัยอยู่ในไฟล์ 'app-init.js'
    initializeApp();

});